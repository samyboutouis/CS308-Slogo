public abstract Command {
    public class MovementCommand(int xCorChange, int yCorChange, int headingChange) {
      doCommand(Turtle turtle) {
        turtle.setX(turtle.getX() + xCor)
        turtle.setY(turtle.getY() + yCor)
        turtle.setHeading(turtle.getHeading() + heading)
      }
    }

    public class PenCommand(boolean penValue) {
      doCommand(Turtle turtle) {
        turtle.setPenValue(penValue)
      }
    }

    public class ClearScreenCommand() {
      doCommand(Turtle turtle) {

      }
    }
  }

  // front-end
  for(Command c : commandList) {
    c.doCommand(turtle)
  }

    SUM fd 20 50
    SUM IF fd 20 [fd 20]
    SUM 5

    stack: LIFO
        SUM
      /     \
    fd
    /
    20


        SUM
      /
    SUM
    / \
    20 20

    stack:

    if fd 50 [ sum 50 50 ]

          if
         /  \
        expr  commands
